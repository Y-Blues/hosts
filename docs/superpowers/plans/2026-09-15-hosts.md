# hosts natif : plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Servir du contenu statique natif : un modèle `@Item` `Host` (préfixe de montage, répertoire, priorité, sécurisation) et un composant `HostServlet`, `IHttpServlet` unique monté sur `/`, qui distribue en interne selon les `Host` connus et sert les fichiers (mimetype, `ETag`/`If-None-Match`, `index.html`, Basic Auth optionnelle).

**Architecture:** Un modèle (`models/host.py`), un composant natif sans interface dédiée qui implémente `IHttpServlet` (`servlet.py`, charge les `Host` à `start()`, dispatch et logique de fichier dans des méthodes privées testables séparément). Aucune modification de `core`/`api` : le contrat `IHttpServlet` actuel (une `path` fixe par servlet, dispatch interne) suffit, comme le confirme la lecture de `core/README.md` et `http_server/servlet.py` (voir spec, section Décisions).

**Tech Stack:** Python ≥ 3.10, uv, Pelix/iPOPO 3, unittest (`IsolatedAsyncioTestCase`), aucune dépendance externe nouvelle (stdlib `mimetypes`, `base64`, `os.path`).

**Spec:** `hosts/docs/superpowers/specs/2026-09-15-hosts-design.md`

## Global Constraints

- Racine du workspace : `/home/yaiba/Documents/yblues`. `hosts` est un dépôt git séparé ; aucune tâche de ce plan ne touche `api`, `core`, `storage`, `endpoints_storage`, `http_server`, `endpoints_service`, `permissions_app`, `scripts`, `scheduler` ou `swagger`.
- Commande de test, lancée depuis `hosts` : `uv run python -m unittest discover -s src/unittest/python`.
- `requires-python = ">=3.10"`.
- **Aucun décorateur sur les classes de composants** (`HostServlet` est une classe native ordinaire qui implémente `IHttpServlet`).
- Tout test HTTP réel utilise un port dans `18150-18159`.
- Committer après la revue de chaque tâche : `git -c user.name="Aurélien Pisu" -c user.email="aurelien.pisu@gmail.com" commit -m "hosts: <message court>"`, jamais pendant qu'un sous-agent travaille encore dans le dépôt, jamais de ligne d'attribution, jamais de push.

## Structure des fichiers

| Fichier | Responsabilité |
|---|---|
| `pyproject.toml` | projet uv, dépendances locales éditables |
| `src/main/python/ycappuccino/hosts/models/host.py` | `Host` |
| `src/main/python/ycappuccino/hosts/servlet.py` | `HostServlet` |
| `example/conf/application.yml`, `example/client/*`, `example/demo/*` | exemple exécutable |
| `README.md` | documentation |

---

### Task 1 : projet uv, suppression du legacy, modèle `Host`

**Files:**
- Modify: `hosts/pyproject.toml` (tout le fichier)
- Modify: `hosts/.gitignore` (déjà `data`, ajouter `.venv`, `__pycache__`, `dist`)
- Delete (`git rm`) : `build.py`, `setup.py`, `src/main/python/ycappuccino/hosts/bundles/` (dossier complet), `src/main/python/ycappuccino/hosts/services/` (dossier complet), `src/main/python/ycappuccino/hosts/bootstrap.py`, `src/main/python/ycappuccino/hosts/conf/` (dossier complet), `src/main/python/ycappuccino/hosts/models/host.py` (ancienne version, remplacée), `src/unittest/python/test_index_endpoint.py`, `test_login_service.py`, `test_tenant_service.py`, `test_core_bootstrap.py`, `test_service_path.py`, `test_replace_service.py` (tous vides, 0 octet, aucun contenu à préserver)
- Create: `hosts/src/main/python/ycappuccino/hosts/__init__.py` (contenu remplacé)
- Create: `hosts/src/main/python/ycappuccino/hosts/models/__init__.py` (vide, déjà présent — vérifier)
- Create: `hosts/src/main/python/ycappuccino/hosts/models/host.py`
- Create: `hosts/src/unittest/python/hosts_fixtures.py`
- Create: `hosts/src/unittest/python/test_host_model.py`

**Interfaces:**
- Consumes: `ycappuccino.api.decorators.Item/Property`, `ycappuccino.api.models.Model`, `ycappuccino.core.decorator_app.App` (déjà construits, voir `storage`/`scheduler`).
- Produces: `Host` (collection `hosts`, item `host`, pluriel `hosts`) avec `path`, `directory`, `priority` (défaut `0`), `secure` (défaut `False`). `hosts_fixtures.create_manager()` : un `Manager` réel sur `MemoryStorage`, même forme que `scheduler_fixtures.create_manager()`.

- [ ] **Step 1: Remove the PyBuilder project and legacy files**

```bash
cd hosts
git rm -q -r build.py setup.py \
  src/main/python/ycappuccino/hosts/bundles \
  src/main/python/ycappuccino/hosts/services \
  src/main/python/ycappuccino/hosts/bootstrap.py \
  src/main/python/ycappuccino/hosts/conf \
  src/main/python/ycappuccino/hosts/models/host.py \
  src/unittest/python/test_index_endpoint.py \
  src/unittest/python/test_login_service.py \
  src/unittest/python/test_tenant_service.py \
  src/unittest/python/test_core_bootstrap.py \
  src/unittest/python/test_service_path.py \
  src/unittest/python/test_replace_service.py
```

Si l'une de ces suppressions est refusée par le système de permissions : ne pas contourner, s'arrêter et le signaler dans le rapport final.

`hosts/pyproject.toml` :

```toml
[project]
name = "ycappuccino-hosts"
version = "0.1.0"
description = "YCappuccino hosts: serves static files from prioritized, optionally Basic-Auth-protected filesystem directories"
requires-python = ">=3.10"
dependencies = [
    "ycappuccino-api",
    "ycappuccino-core",
    "ycappuccino-storage",
]

[build-system]
requires = ["uv_build>=0.12.13,<0.13"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "ycappuccino.hosts"
module-root = "src/main/python"

[tool.uv.sources]
ycappuccino-api = { path = "../api", editable = true }
ycappuccino-core = { path = "../core", editable = true }
ycappuccino-storage = { path = "../storage", editable = true }
```

`hosts/.gitignore` :

```
data
.venv
__pycache__
dist
```

`hosts/src/main/python/ycappuccino/hosts/__init__.py` :

```python
"""serves static files from prioritized, optionally Basic-Auth-protected filesystem directories"""
```

Run (depuis `hosts`) : `uv sync`
Expected: environnement créé, les trois dépendances locales installées en éditable.

- [ ] **Step 2: Write the fixtures and the failing test**

`hosts/src/unittest/python/hosts_fixtures.py` :

```python
"""
Fixtures shared by the hosts tests: a real Manager on MemoryStorage, no framework.
"""

import os
import tempfile

from ycappuccino.storage.files import LocalFileStore
from ycappuccino.storage.items import ItemManager
from ycappuccino.storage.manager import Manager
from ycappuccino.storage.memory import MemoryStorage

# importing the model registers it with ItemManager
from ycappuccino.hosts.models import host  # noqa: F401


def create_manager():
    """manager on a memory storage; the caller removes the returned directory"""
    directory = tempfile.mkdtemp()
    manager = Manager(MemoryStorage(), ItemManager(), [], [], LocalFileStore(os.path.join(directory, "files")))
    return manager, directory
```

`hosts/src/unittest/python/test_host_model.py` :

```python
import shutil
import unittest

from hosts_fixtures import create_manager

from ycappuccino.hosts.models.host import Host


class TestHost(unittest.IsolatedAsyncioTestCase):

    async def asyncSetUp(self):
        self.manager, directory = create_manager()
        self.addCleanup(shutil.rmtree, directory, True)

    async def test_round_trip_through_the_manager(self):
        host = Host()
        host.id("default")
        host.path("/")
        host.directory("client")
        host.priority(5)
        host.secure(True)

        await self.manager.up_sert_model(host)
        stored = (await self.manager.get_one("host", "default")).get_storage_model()

        self.assertEqual(stored["path"], "/")
        self.assertEqual(stored["directory"], "client")
        self.assertEqual(stored["priority"], 5)
        self.assertIs(stored["secure"], True)

    async def test_defaults(self):
        host = Host()

        self.assertEqual(host.get_storage_model().get("priority", 0), 0)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 3: Run test to verify it fails**

Run (depuis `hosts`) : `uv run python -m unittest discover -s src/unittest/python -p test_host_model.py`
Expected: `ModuleNotFoundError: No module named 'ycappuccino.hosts.models.host'`.

- [ ] **Step 4: Implement**

`hosts/src/main/python/ycappuccino/hosts/models/host.py` :

```python
"""
Host: a URL mount prefix served from one filesystem directory, with priority and
optional HTTP Basic Auth. See spec (2026-09-15-hosts-design.md) for the simplifications
versus the legacy model (single `directory`, no `type`/`core`/pyscript fields).
"""

from ycappuccino.api.decorators import Item, Property
from ycappuccino.api.models import Model
from ycappuccino.core.decorator_app import App


@App(name="ycappuccino_hosts")
@Item(collection="hosts", name="host", plural="hosts")
class Host(Model):

    def __init__(self, a_dict=None):
        super().__init__(a_dict)
        self._path = None
        self._directory = None
        self._priority = 0
        self._secure = False

    @Property(name="path")
    def path(self, a_value):
        self._path = a_value

    @Property(name="directory")
    def directory(self, a_value):
        self._directory = a_value

    @Property(name="priority", type="integer")
    def priority(self, a_value):
        self._priority = a_value

    @Property(name="secure", type="boolean")
    def secure(self, a_value):
        self._secure = a_value
```

(Vérifier la forme exacte de `@Property(type=...)` dans `storage/README.md` / un modèle existant tel que `endpoints_storage` avant d'implémenter — s'aligner sur l'usage réel du dépôt plutôt que d'inventer un mot-clé.)

- [ ] **Step 5: Run tests to verify they pass**

Run (depuis `hosts`) : `uv run python -m unittest discover -s src/unittest/python`
Expected: `OK`.

---

### Task 2 : `HostServlet` — dispatch et service de fichiers (logique pure, sans framework)

**Files:**
- Create: `hosts/src/main/python/ycappuccino/hosts/servlet.py`
- Create: `hosts/src/unittest/python/test_servlet_dispatch.py`

**Interfaces:**
- Consumes (Task 1) : `hosts_fixtures.create_manager`, `Host`. `ycappuccino.api.storage.IManager`, `ycappuccino.api.http.HttpRequest/HttpResponse/IHttpServlet`, `ycappuccino.api.core.IActivityLogger/IConfiguration`, `ycappuccino.api.core_base.YCappuccinoType`.
- Produces: `HostServlet(manager, configuration, logger, path="/")`, `IHttpServlet` natif. `await servlet.start()` charge les `Host` (via un faux `IManager` en test, un vrai en intégration). `await servlet.handle(request)` teste tout le comportement (voir spec section 6).

- [ ] **Step 1: Write the failing tests**

`hosts/src/unittest/python/test_servlet_dispatch.py` (esquisse — l'implémenteur complète chaque cas de la spec section 6) :

```python
import base64
import os
import shutil
import tempfile
import unittest
from unittest import mock

from ycappuccino.api.http import HttpRequest
from ycappuccino.hosts.servlet import HostServlet


def _request(path, headers=None):
    return HttpRequest(
        method="GET", path=path, prefix="/", sub_path=path, query={},
        headers=headers or {},
    )


class FakeModel:
    def __init__(self, document):
        self._document = document

    def get_storage_model(self):
        return self._document


class FakeManager:
    def __init__(self, documents):
        self._documents = documents

    async def get_many(self, item_id, params=None, subject=None):
        return [FakeModel(document) for document in self._documents]

    async def start(self):
        pass

    async def stop(self):
        pass


class FakeConfiguration:
    def __init__(self, values):
        self._values = values

    def get(self, key, default):
        return self._values.get(key, default)


class TestHostServletDispatch(unittest.IsolatedAsyncioTestCase):

    async def asyncSetUp(self):
        self.directory = tempfile.mkdtemp()
        self.addCleanup(shutil.rmtree, self.directory, True)
        os.makedirs(os.path.join(self.directory, "sub"), exist_ok=True)
        with open(os.path.join(self.directory, "index.html"), "w") as file:
            file.write("<html>home</html>")
        with open(os.path.join(self.directory, "style.css"), "w") as file:
            file.write("body {}")
        with open(os.path.join(self.directory, "sub", "index.html"), "w") as file:
            file.write("<html>sub</html>")

    def _servlet(self, hosts, configuration=None):
        manager = FakeManager(hosts)
        return HostServlet(manager, configuration, mock.Mock())

    async def test_serves_a_text_file_with_its_mimetype(self):
        servlet = self._servlet([{"_id": "default", "path": "/", "directory": self.directory, "priority": 0, "secure": False}])
        await servlet.start()

        response = await servlet.handle(_request("/style.css"))

        self.assertEqual(response.status, 200)
        self.assertEqual(response.body, b"body {}")
        self.assertEqual(response.content_type, "text/css")

    async def test_root_path_serves_index_html(self):
        servlet = self._servlet([{"_id": "default", "path": "/", "directory": self.directory, "priority": 0, "secure": False}])
        await servlet.start()

        response = await servlet.handle(_request("/"))

        self.assertEqual(response.status, 200)
        self.assertIn(b"home", response.body)

    async def test_directory_like_path_serves_its_index_html(self):
        servlet = self._servlet([{"_id": "default", "path": "/", "directory": self.directory, "priority": 0, "secure": False}])
        await servlet.start()

        response = await servlet.handle(_request("/sub/"))

        self.assertIn(b"sub", response.body)

    async def test_no_matching_host_is_404(self):
        servlet = self._servlet([{"_id": "app", "path": "/app", "directory": self.directory, "priority": 0, "secure": False}])
        await servlet.start()

        response = await servlet.handle(_request("/other/file.txt"))

        self.assertEqual(response.status, 404)

    async def test_prefix_does_not_match_partially(self):
        servlet = self._servlet([{"_id": "app", "path": "/pyscriptcore", "directory": self.directory, "priority": 0, "secure": False}])
        await servlet.start()

        response = await servlet.handle(_request("/pyscriptcoreextra/style.css"))

        self.assertEqual(response.status, 404)

    async def test_missing_file_is_404(self):
        servlet = self._servlet([{"_id": "default", "path": "/", "directory": self.directory, "priority": 0, "secure": False}])
        await servlet.start()

        response = await servlet.handle(_request("/missing.txt"))

        self.assertEqual(response.status, 404)

    async def test_path_traversal_is_rejected(self):
        servlet = self._servlet([{"_id": "default", "path": "/", "directory": self.directory, "priority": 0, "secure": False}])
        await servlet.start()

        response = await servlet.handle(_request("/../../etc/passwd"))

        self.assertEqual(response.status, 404)

    async def test_higher_priority_host_wins_on_overlap(self):
        servlet = self._servlet([
            {"_id": "root", "path": "/", "directory": self.directory, "priority": 0, "secure": False},
            {"_id": "sub", "path": "/sub", "directory": os.path.join(self.directory, "sub"), "priority": 1, "secure": False},
        ])
        await servlet.start()

        response = await servlet.handle(_request("/sub/"))

        self.assertIn(b"sub", response.body)

    async def test_etag_round_trip_returns_304(self):
        servlet = self._servlet([{"_id": "default", "path": "/", "directory": self.directory, "priority": 0, "secure": False}])
        await servlet.start()

        first = await servlet.handle(_request("/style.css"))
        etag = first.headers["ETag"]

        second = await servlet.handle(_request("/style.css", headers={"if-none-match": etag}))

        self.assertEqual(second.status, 304)

    async def test_secure_host_without_credentials_is_401(self):
        servlet = self._servlet(
            [{"_id": "admin", "path": "/", "directory": self.directory, "priority": 0, "secure": True}],
            configuration=FakeConfiguration({"admin.login": "alice", "admin.password": "secret"}),
        )
        await servlet.start()

        response = await servlet.handle(_request("/style.css"))

        self.assertEqual(response.status, 401)
        self.assertIn("Basic", response.headers["WWW-Authenticate"])

    async def test_secure_host_with_wrong_credentials_is_401(self):
        servlet = self._servlet(
            [{"_id": "admin", "path": "/", "directory": self.directory, "priority": 0, "secure": True}],
            configuration=FakeConfiguration({"admin.login": "alice", "admin.password": "secret"}),
        )
        await servlet.start()

        header = "Basic " + base64.standard_b64encode(b"alice:wrong").decode()
        response = await servlet.handle(_request("/style.css", headers={"authorization": header}))

        self.assertEqual(response.status, 401)

    async def test_secure_host_with_correct_credentials_is_200(self):
        servlet = self._servlet(
            [{"_id": "admin", "path": "/", "directory": self.directory, "priority": 0, "secure": True}],
            configuration=FakeConfiguration({"admin.login": "alice", "admin.password": "secret"}),
        )
        await servlet.start()

        header = "Basic " + base64.standard_b64encode(b"alice:secret").decode()
        response = await servlet.handle(_request("/style.css", headers={"authorization": header}))

        self.assertEqual(response.status, 200)

    async def test_secure_host_without_configuration_is_always_401(self):
        servlet = self._servlet(
            [{"_id": "admin", "path": "/", "directory": self.directory, "priority": 0, "secure": True}],
            configuration=None,
        )
        await servlet.start()

        header = "Basic " + base64.standard_b64encode(b"anyone:anything".encode()).decode()
        response = await servlet.handle(_request("/style.css", headers={"authorization": header}))

        self.assertEqual(response.status, 401)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: Run test to verify it fails**

Run (depuis `hosts`) : `uv run python -m unittest discover -s src/unittest/python -p test_servlet_dispatch.py`
Expected: `ModuleNotFoundError`.

- [ ] **Step 3: Implement**

`hosts/src/main/python/ycappuccino/hosts/servlet.py` — s'appuyer sur la spec (sections 2 et 3) pour la forme exacte ; points obligatoires :

- `start()` : `await self._manager.get_many("host", subject=None)`, construit `self._hosts` = liste de dicts `{"id", "path", "directory" (résolu en absolu via `os.path.abspath`), "priority", "secure"}` triée par `priority` décroissante.
- `_find_host(path)` : premier `Host` dont `path == "/"` ou `path == host["path"]` ou `path.startswith(host["path"] + "/")`.
- `_resolve_file(host, request_path)` : chemin relatif, jointure, résolution `index.html`, vérification `os.path.realpath(candidate).startswith(os.path.realpath(host["directory"]))`.
- `_check_auth(host, headers)` : voir spec section 3, `_decode_basic`.
- `handle()` : orchestre les trois ci-dessus, calcule `ETag`/`If-None-Match`, lit le fichier en `rb`, devine le mimetype, construit la `HttpResponse`. Toute exception inattendue (I/O) est journalisée (`self._logger.exception(...)`) et renvoie `500`.

- [ ] **Step 4: Run tests to verify they pass**

Run (depuis `hosts`) : `uv run python -m unittest discover -s src/unittest/python`
Expected: `OK`.

---

### Task 3 : intégration au framework, exemple, README et vérification finale

**Files:**
- Test: `hosts/src/unittest/python/test_hosts_framework.py`
- Test: `hosts/src/unittest/python/test_readme.py`
- Create: `hosts/README.md`
- Modify: `hosts/example/conf/application.yml` (tout le fichier)
- Create: `hosts/example/client/index.html`, `hosts/example/client/style.css`
- Create: `hosts/example/demo/__init__.py`, `hosts/example/demo/bootstrap.py`

**Interfaces:**
- Consumes : tout ce qui précède.

- [ ] **Step 1: Write the integration test**

`hosts/src/unittest/python/test_hosts_framework.py` (esquisse) :

```python
import base64
import os
import unittest
import urllib.error
import urllib.request

from ycappuccino.core.framework import Framework
from ycappuccino.core.testing import TemporaryApplication, wait_until

PORT = 18150

APPLICATION = {
    "conf/application.yml": """
        name: hoststest
        bundle_prefix:
          - ycappuccino.storage
          - ycappuccino.hosts
          - PACKAGE
        layers:
          ycappuccino_storage_memory:
            active: true
        config:
          http_server:
            active: true
            port: {port}
            ip: localhost
          shell:
            console: false
    """.replace("{port}", str(PORT)),
    "conf/config.properties": "admin.login = alice\nadmin.password = secret\n",
    "PACKAGE/__init__.py": "",
    "PACKAGE/bootstrap.py": """
        from ycappuccino.api.core_base import YCappuccinoComponent
        from ycappuccino.api.storage import IManager
        from ycappuccino.hosts.models.host import Host


        class HostBootstrap(YCappuccinoComponent):
            def __init__(self, manager: IManager):
                self._manager = manager

            async def stop(self):
                pass

            async def start(self):
                default_host = Host()
                default_host.id("default")
                default_host.path("/")
                default_host.directory(__file__.rsplit("/", 1)[0] + "/client")
                default_host.priority(0)
                default_host.secure(False)
                await self._manager.up_sert_model(default_host, subject=None)

                admin_host = Host()
                admin_host.id("admin")
                admin_host.path("/admin")
                admin_host.directory(__file__.rsplit("/", 1)[0] + "/client")
                admin_host.priority(1)
                admin_host.secure(True)
                await self._manager.up_sert_model(admin_host, subject=None)
    """,
    "PACKAGE/client/index.html": "<html>hello</html>",
}


class TestHostsInFramework(unittest.TestCase):

    @classmethod
    def setUpClass(cls):
        cls.app = TemporaryApplication(APPLICATION).open()
        cls.addClassCleanup(cls.app.close)
        cls.framework = Framework()
        cls.framework.init(cls.app.yml_path)
        cls.addClassCleanup(cls.framework.stop)
        wait_until(lambda: cls.framework.context.get_service_reference("HostServlet"))
        # HostServlet loads its hosts at start(): wait for the bootstrapped host to be servable
        wait_until(lambda: cls._get("/")[0] == 200, timeout=5)

    @classmethod
    def _get(cls, path, headers=None):
        req = urllib.request.Request(f"http://localhost:{PORT}{path}", headers=headers or {})
        try:
            with urllib.request.urlopen(req, timeout=5) as response:
                return response.status, response.read(), dict(response.headers)
        except urllib.error.HTTPError as error:
            with error:
                return error.code, error.read(), dict(error.headers)

    def test_static_file_is_served(self):
        status, body, _ = self._get("/")

        self.assertEqual(status, 200)
        self.assertIn(b"hello", body)

    def test_unknown_path_is_404(self):
        status, _, _ = self._get("/nope.txt")

        self.assertEqual(status, 404)

    def test_secure_host_requires_basic_auth(self):
        status, _, _ = self._get("/admin/")
        self.assertEqual(status, 401)

        header = "Basic " + base64.standard_b64encode(b"alice:secret").decode()
        status, body, _ = self._get("/admin/", headers={"Authorization": header})
        self.assertEqual(status, 200)

    def test_etag_returns_304(self):
        _, _, headers = self._get("/")
        etag = headers["ETag"]

        status, _, _ = self._get("/", headers={"If-None-Match": etag})

        self.assertEqual(status, 304)


if __name__ == "__main__":
    unittest.main()
```

Note pour l'implémenteur : si `HostBootstrap` (qui écrit les `Host`) démarre après `HostServlet` (qui les lit une seule fois à son propre `start()`), le premier `wait_until(lambda: cls._get("/")[0] == 200)` absorbe ce non-déterminisme en réessayant — comme documenté dans le plan `scheduler` pour un problème analogue (`TaskBootstrap`/`ScheduleExecutor`). Si en pratique ce n'est pas suffisant (le servlet a déjà chargé une liste vide avant que le bootstrap n'écrive), le corriger en ajoutant une dépendance constructeur direct de `HostServlet` sur rien de plus — la solution retenue si besoin est de garder le test tel quel avec un `wait_until` à `timeout` plus généreux, PAS de rechargement à chaud (hors périmètre, voir spec).

- [ ] **Step 2: Run the integration test**

Run (depuis `hosts`) : `uv run python -m unittest discover -s src/unittest/python -p test_hosts_framework.py`
Expected: `OK`.

- [ ] **Step 3: Write the README**

`hosts/README.md` (même structure que `scheduler/README.md`) : mise en place (`uv add --editable ../hosts`, `bundle_prefix`), modèle `Host` avec exemple `up_sert_model`, section « Un seul servlet, plusieurs montages » (résume la décision de la spec), section Basic Auth (`<id>.login`/`<id>.password`, pas de défaut), section « Tester avec hosts » (construction directe de `HostServlet` avec un faux `IManager` et un vrai répertoire temporaire — reprendre un sous-ensemble de `test_servlet_dispatch.py`), section « Développer hosts ».

- [ ] **Step 4: Write the README test**

`hosts/src/unittest/python/test_readme.py` : reprend exactement l'exemple de la section « Tester avec hosts » du README (le test est la source de vérité ; ajuster le README si nécessaire pour qu'il corresponde caractère pour caractère à ce test).

- [ ] **Step 5: Run the README test**

Run (depuis `hosts`) : `uv run python -m unittest discover -s src/unittest/python -p test_readme.py`
Expected: `OK`.

- [ ] **Step 6: Rebuild the example**

`hosts/example/conf/application.yml` :

```yaml
---
name: hosts-demo
bundle_prefix:
  - ycappuccino.storage
  - ycappuccino.hosts
  - demo
layers:
  ycappuccino_storage_memory:
    active: true
config:
  http_server:
    active: true
    port: 8080
    ip: localhost
```

`hosts/example/client/index.html`, `hosts/example/client/style.css` : contenu statique minimal.

`hosts/example/demo/__init__.py` : fichier vide.

`hosts/example/demo/bootstrap.py` : `HostBootstrap` (`YCappuccinoComponent`, dépend d'`IManager`) qui crée le `Host` par défaut (`path="/"`, `directory` pointant vers `example/client`, `priority=0`, `secure=False`) à son `start()`, comme dans le test d'intégration.

- [ ] **Step 7: Final verification**

Run (depuis `hosts`) : `uv run python -m unittest discover -s src/unittest/python`
Expected: `OK` pour tous les tests (`test_host_model`, `test_servlet_dispatch`, `test_hosts_framework`, `test_readme`).

Puis vérifier que l'exemple démarre sans erreur et sert bien `index.html` :

```bash
cd example && timeout 5 uv run --project .. ycappuccino & sleep 2 && curl -s http://localhost:8080/ ; cd ..
```

Expected: le contenu de `client/index.html`, pas de traceback dans la sortie ni dans `example/data/log/Log-Activity-main.log`.
